async function main() {
    // var data = new DataLayer;
    // data.champList();

    // var bussines = new BusinessLayer;
    // bussines.seperateMatches();
    // bussines.getMatchDetails();
    // bussines.champListRework();

    var presentationObject = new Presentation;
    presentationObject.displayGames();
}

main();
